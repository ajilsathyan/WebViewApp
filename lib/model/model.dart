import 'package:flutter/material.dart';

class DataClass {
  String webappName;
  String url;
  DataClass({this.webappName, this.url});
}

class AppIcons {
  IconData iconName;
  Color colorForIcon;

  AppIcons({this.iconName, this.colorForIcon});
}

